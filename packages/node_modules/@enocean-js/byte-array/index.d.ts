export type ByteArrayConstructor =  (number|string|Array<number|string>);

declare module '@enocean-js/byte-array' {
    export class ByteArray extends Array {
        set (arr, offset?:number):void ;

        getValue (bitOffset: number, bitLength: number): number ;

        setValue (value: number, offset: number, bitLength: number) : this;

        setSingleBit (offset: number, value: 0|1);

        getSingleBit (offset: number) : 0|1;

        toString (radix?: 'bin'| 'dec'| 'hex'|2|10|16);
        static create(length: number): ByteArray;
        static from(...a: ByteArrayConstructor[]):ByteArray;
    }
}
